# Delivered app outputs — not reference material.
# Excluding prevents the agent from reading past deliveries
# and inferring wrong UI behaviour or components from them.
apps/

# Client spec files — read explicitly when needed, not auto-indexed.
# Prevents past specs from bleeding domain vocabulary or UI patterns
# into a new build. The agent reads the relevant spec via an explicit
# Read call when the user points to it.
specs/
